repos:
  - repo: local
    hooks:
      - id: pytest-unit
        name: pytest (unit tests)
        language: system
        entry: .venv/bin/pytest tests/ -m "not integration" -q --tb=short
        pass_filenames: false
        always_run: true
